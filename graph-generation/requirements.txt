# Core dependencies
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0

# LLM providers (choose one or both)
anthropic>=0.40.0          # For Claude (updated to fix compatibility issues)
openai>=1.12.0             # For OpenAI
httpx>=0.27.0              # Required for Anthropic compatibility

# Utilities
requests==2.31.0
jsonschema==4.19.1         # For schema validation
